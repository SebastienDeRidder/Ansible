---
- hosts: all
  become: "{{ inventory_hostname != 'OpenVPN' }}"
  become_method: su  # Use su as the privilege escalation method

  tasks:
    - name: update apt packages
      apt:
        upgrade: yes
        update_cache: yes
      when: inventory_hostname != 'OpenVPN'  # Conditionally run the task based on the host